/**
 * CENF Core Infrastructure (TypeScript) — Public API
 *
 * This is the entry point for the core-cenf-ts package.
 * Managers are exported as they are implemented.
 *
 * @module core-cenf-ts
 */

// Placeholder — managers will be exported as implemented
export const VERSION = '0.1.0';
